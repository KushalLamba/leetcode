#include<iostream>
#include<vector>
using namespace std;
vector<vector<string>> palidrome(string s1,string s2)
{
    if(s1.empty() || s2.empty())
    {
        
    }
}
int main()
{
    cout<<"Enter String: "<<endl;
    string s;
    cin>>s;
}